namespace RoomReservationSystemApp;

public class Trash
{
    /*public bool ValidationAvailableRoomNumber(int roomNumber)
{
    var rooms = _repository.GetAll().Where(r => r.IsAvailable).ToList();
    return rooms.Any(r => r.Number == roomNumber);
}*/
}